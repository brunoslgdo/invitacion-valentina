include ../config/variables
doctype html
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Mis XIV, Valentina Valencia Aguirre
        link(href=`${pathCss}index.css` rel="stylesheet")
    body
        block content
        script(src=`${pathJs}countdown.js`) 